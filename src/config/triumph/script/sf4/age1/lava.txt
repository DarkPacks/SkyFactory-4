setTitle("Lava Bucket")

setIcon("minecraft:lava_bucket")

setDescription("Create lava by using Cobble in a Cauldron collect it in a clay bucket or regular bucket. To melt cobble, you need to have a torch, fire, magma block, or lava directly under the Cauldron")

addParent("sf4:age1/cobblestone")
addParent("sf4:age1/cauldron")

drawDirectLines(true)

//Criteria
criteria = addCriteria("hasRock", "minecraft:inventory_changed")
criteria.addItem("ceramics:clay_bucket", 0, {FluidName:"lava",Amount:1000})

criteria1 = addCriteria("hasRock1", "minecraft:inventory_changed")
criteria1.addItem("minecraft:lava_bucket")

setRequirements("any")

//Rewards
setRewardExperience(4)

alwaysVisible()
setPos(1,77)