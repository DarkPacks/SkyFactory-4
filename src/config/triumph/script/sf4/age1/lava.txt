setTitle("Lava Bucket")

setIcon("minecraft:lava_bucket")

setDescription("Create lava by using Cobble in a Cauldron. Collect it in a Clay or Regular bucket. To melt cobble, you need to have a torch, fire, magma block, or lava directly under the Cauldron")

addParent("sf4:age1/cobblestone")
addParent("sf4:age1/cauldron")

drawDirectLines(true)

setRequirements("any")

//Criteria
criteria1 = addCriteria("hasRock1", "minecraft:inventory_changed")
criteria1.addItem(<ceramics:clay_bucket, meta:0, count:1, nbt:{fluids:{FluidName:"lava",Amount:1000}}>)

criteria2 = addCriteria("hasRock2", "minecraft:inventory_changed")
criteria2.addItem("minecraft:lava_bucket")

//Rewards
setRewardExperience(4)

alwaysVisible()
setPos(1,76)