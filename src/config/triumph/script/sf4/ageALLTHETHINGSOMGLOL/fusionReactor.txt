setTitle("Don't Overreact")

setIcon(<matteroverdrive:gravitational_anomaly>)

setDescription("Create a Matter Overdrive Fusion Reactor that produces at least 15,000FE per tick. Open the center block on the bottom of the controller to complete the advancement")

addParent("sf4:ageALLTHETHINGSOMGLOL/root")

hideLines()
alwaysVisible()

//Criteria
criteria = addCriteria("Right Click Fusion Reactor Generating 15k FE/t", "triumph:right_click_block")
criteria.setBlock(<matteroverdrive:fusion_reactor_controller,tile:{EnergyPerTick:15000~}>)

//Rewards
setRewardExperience(4)

setPos(64,101)