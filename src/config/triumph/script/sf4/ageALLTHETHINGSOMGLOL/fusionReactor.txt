setTitle("Fusion Reactor")

setIcon(<matteroverdrive:gravitational_anomaly>)

setDescription("Create a Matter Overdrive Fusion Reactor that produces at least 15,000FE per tick")

setParent("sf4:ageALLTHETHINGSOMGLOL/root")

drawDirectLines(true)
alwaysVisible()

criteria = addCriteria("clickedBlock", "triumph:right_click_block")
criteria.setBlock(<matteroverdrive:fusion_reactor_controller,tile:{EnergyPerTick:15000~}>)

//Rewards
setRewardExperience(4)

setPos(32,128)