setTitle("Gotta Go Fast")

setIcon(<mekanism:speedupgrade>)

setDescription("Craft a Mekanism Speed upgrade to speed up your machines. Careful, the faster the machine the higher the power usage")

addParent("sf4:ageEnhancement/root")

hideLines()

//Criteria
criteria = addCriteria("Obtain Mekanism Speed Upgrade", "minecraft:inventory_changed")
criteria.addItem(<mekanism:speedupgrade>)

setRewardExperience(8)

alwaysVisible()
setPos(-113,108)