setTitle("Let the Hunt Begin")

setIcon(<huntingdim:portal>)

setDescription("Travel to the Hunting Dimension. Build the Hunting Dimension portal using Hunting Dimension Frame Blocks. Light it by Right-Clicking with a sword, bow, or flint and steel")

addParent("sf4:ageExploration/root")

drawDirectLines(true)

criteria = addCriteria("Entered Hunting Dimension", "minecraft:changed_dimension")
criteria.setDimensionDestination("hunting_dim")

hideLines()
alwaysVisible()
setPos(7,253)