setTitle("It Fits, We Ship")

setIcon(<bdsandm:shipping_container>)

setDescription("Craft a 'Barrels, Drums, Storage, and More' Shipping Container. This container allows inserting most blocks that can hold items, liquids, or power")

addParent("sf4:ageStorage/bdsandmMetalCrate")
addParent("sf4:ageStorage/bdsandmMetalBarrel")

drawDirectLines(true)

//Criteria
criteria = addCriteria("Obtain Shipping Container", "minecraft:inventory_changed")
criteria.addItem(<bdsandm:shipping_container>)

//Rewards
setRewardExperience(4)

alwaysVisible()
setPos(-184,176)